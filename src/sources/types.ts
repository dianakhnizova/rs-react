export type BookData = {
  id: string;
  title?: string;
  description?: string;
  image?: string;
  authors?: string;
  pageCount?: number | string;
  printType?: string;
};

export type MyData = {
  label: string;
  data: string;
};
