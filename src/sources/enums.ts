export enum LocalStorage {
  SEARCH_KEY = 'searchInput',
  THEME_KEY = 'theme',
}

export enum Theme {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum BookApiTags {
  BOOKS = 'books',
  BOOK_LIST = 'bookList',
  BOOK_BY_ID = 'bookById',
}

export enum PagePath {
  root = '/1',
  aboutPage = '/about',
}
