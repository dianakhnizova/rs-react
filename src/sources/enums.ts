export enum Variant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}

export enum ResourceStatus {
  PENDING = 'pending',
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum InputType {
  CHECKBOX = 'checkbox',
  TEXT = 'text',
}

export enum InputId {
  ONE = 'one',
  TWO = 'two',
  THREE = 'three',
  FOUR = 'four',
  FIVE = 'five',
}
